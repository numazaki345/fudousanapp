%h1 物件一覧

= link_to '新規登録', new_building_path, class: 'btn btn-primary'

.md-3
%table.table.table-hover
  %thead.thead-default
    %tr
      %th= Building.human_attribute_name('住所')
      %th= Building.human_attribute_name('登録日')
  %tbody
    - @buildings.each do |building|
      %tr
        %td= link_to building.address, building
        %td= building.created_at
        %td
          = button_to '削除', building, method: :delete, class: 'btn btn-danger'
